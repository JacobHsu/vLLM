# vLLM 學習計畫

追蹤整體學習進度用的地圖,每學一個新主題就在這裡更新狀態、補連結。逐篇翻譯的官方文件放在 [vLLM/](vLLM/) 資料夾,本機實測紀錄放在根目錄的 [quickstart.md](quickstart.md)。

## 已完成

- [x] **Quickstart(快速入門)**
  官方文件翻譯:[vLLM/quickstart.zh.md](vLLM/quickstart.zh.md)
  本機實測紀錄(WSL2 + RTX 5080 Blackwell 安裝、踩坑、離線推論/線上服務驗證):[quickstart.md](quickstart.md)

- [x] **Engine Arguments(啟動參數)**
  官方文件:[docs.vllm.ai/configuration/engine_args](https://docs.vllm.ai/en/latest/configuration/engine_args/)
  實用子集翻譯:[vLLM/configuration/engine_args.zh.md](vLLM/configuration/engine_args.zh.md)

- [x] **Quantization(量化)— 索引頁 + 實測**
  官方文件:[docs.vllm.ai/features/quantization](https://docs.vllm.ai/en/latest/features/quantization/)
  翻譯(核心內容,未含個別格式細節頁):[vLLM/features/quantization.zh.md](vLLM/features/quantization.zh.md)
  本機實測紀錄:[quantization-benchmark.md](quantization-benchmark.md)
  重要發現:官方硬體相容表沒列 Blackwell;實測 FP8(llm-compressor)沒問題,7B 模型不量化在這張 16GB 卡上會啟動失敗;**NVFP4 目前啟動失敗**,原因是 vLLM/FlashInfer 對 SM120 消費級顯卡的 NVFP4 GEMM kernel 支援還沒做完(上游未解 issue,非設定問題)

- [x] **用 `llm-eval-harness` 測基準效能** — 已在 [quantization-benchmark.md](quantization-benchmark.md) 裡用 `speed_probe.py` 實測三個模型的 tok/s、TTFT

- [x] **併發能力實測** — 已在 [quantization-benchmark.md](quantization-benchmark.md) 用 `concurrency_probe.py` 測到 1024 併發成功率仍 100%,證實啟動時算出的理論值(2.14x)只適用於「滿 32K 上下文」的最壞情況,短訊息場景撐得住的量遠遠更高

- [x] **Structured Outputs(結構化輸出)— 文件翻譯**
  官方文件:[docs.vllm.ai/features/structured_outputs](https://docs.vllm.ai/en/latest/features/structured_outputs/)
  完整翻譯:[vLLM/features/structured_outputs.zh.md](vLLM/features/structured_outputs.zh.md)

- [x] **Tool Calling(工具呼叫)— 文件翻譯**
  官方文件:[docs.vllm.ai/features/tool_calling](https://docs.vllm.ai/en/latest/features/tool_calling/)
  完整翻譯(除了結尾的自訂 parser 開發文件):[vLLM/features/tool_calling.zh.md](vLLM/features/tool_calling.zh.md)
  備註:Qwen 系列(我們一直在測的 `Qwen2.5-*`)直接用 `--tool-call-parser hermes` 就能開 tool calling,不用額外找聊天範本

## 進行中

- [ ] **Structured Outputs 本機實測** — 文件翻完了,還沒實際跑過(`choice`/`regex`/`json`/`grammar` 這幾種模式,搭配現有的 FP8 7B 或 1.5B 伺服器測)
- [ ] **Tool Calling 本機實測** — 文件翻完了,還沒實際跑過(用現有的 Qwen2.5 模型 + `--enable-auto-tool-choice --tool-call-parser hermes` 測 quickstart.zh.md 裡那個 `get_weather` 範例)

## 待學(依優先順序)

1. **原理面(選讀)**:理解 vLLM 為什麼快
   [design/paged_attention](https://docs.vllm.ai/en/latest/design/paged_attention/)、[design/prefix_caching](https://docs.vllm.ai/en/latest/design/prefix_caching/)

## 追蹤中(不急,定期回頭看)

- NVFP4 在 SM120 消費級 Blackwell 上的上游支援進度(見 [quantization-benchmark.md](quantization-benchmark.md) 的 NVFP4 專節列出的幾個 issue),修好後重測

## 文件慣例

- `vLLM/` 底下依照官網的 URL 路徑建子資料夾(例如 `configuration/engine_args.zh.md` 對應 `docs.vllm.ai/en/latest/configuration/engine_args/`),篇幅大的頁面只翻實用子集,結尾附官方連結
- 根目錄的 `quickstart.md`、`quantization-benchmark.md` 這類文件:這台機器上的實測紀錄,跟官方文件翻譯分開放
