# vLLM 學習計畫

追蹤整體學習進度用的地圖,每學一個新主題就在這裡更新狀態、補連結。逐篇翻譯的官方文件放在 [vLLM/](vLLM/) 資料夾,本機實測紀錄放在根目錄的 [quickstart.md](quickstart.md)。

## 已完成

- [x] **Quickstart(快速入門)**
  官方文件翻譯:[vLLM/quickstart.zh.md](vLLM/quickstart.zh.md)
  本機實測紀錄(WSL2 + RTX 5080 Blackwell 安裝、踩坑、離線推論/線上服務驗證):[quickstart.md](quickstart.md)

- [x] **Engine Arguments(啟動參數)**
  官方文件:[docs.vllm.ai/configuration/engine_args](https://docs.vllm.ai/en/latest/configuration/engine_args/)
  實用子集翻譯:[vLLM/configuration/engine_args.zh.md](vLLM/configuration/engine_args.zh.md)

- [x] **Quantization(量化)— 索引頁 + 實測**
  官方文件:[docs.vllm.ai/features/quantization](https://docs.vllm.ai/en/latest/features/quantization/)
  翻譯(核心內容,未含個別格式細節頁):[vLLM/features/quantization.zh.md](vLLM/features/quantization.zh.md)
  本機實測紀錄:[quantization-benchmark.md](quantization-benchmark.md)
  重要發現:官方硬體相容表沒列 Blackwell;實測 FP8(llm-compressor)沒問題,7B 模型不量化在這張 16GB 卡上會啟動失敗

- [x] **用 `llm-eval-harness` 測基準效能** — 已在 [quantization-benchmark.md](quantization-benchmark.md) 裡用 `speed_probe.py` 實測三個模型的 tok/s、TTFT

## 待學(依優先順序)

1. **NVFP4 量化實測** — Blackwell 這代主推的格式,還沒測過,對照已有的 FP8 數據(見 [quantization-benchmark.md](quantization-benchmark.md) 的下一步)

2. **併發能力實測** — 用 `llm-eval-harness` 的 `concurrency_probe.py` 實際測出 FP8 7B 在這張卡上的併發上限,而不是只看 vLLM 啟動時算出的理論值

3. **Structured Outputs / Tool Calling** — 如果要拿 vLLM 接自己寫的應用,會實際用到的 API 能力
   官方文件:[docs.vllm.ai/features/structured_outputs](https://docs.vllm.ai/en/latest/features/structured_outputs/)、[docs.vllm.ai/features/tool_calling](https://docs.vllm.ai/en/latest/features/tool_calling/)

4. **原理面(選讀)**:理解 vLLM 為什麼快
   [design/paged_attention](https://docs.vllm.ai/en/latest/design/paged_attention/)、[design/prefix_caching](https://docs.vllm.ai/en/latest/design/prefix_caching/)

## 文件慣例

- `vLLM/` 底下依照官網的 URL 路徑建子資料夾(例如 `configuration/engine_args.zh.md` 對應 `docs.vllm.ai/en/latest/configuration/engine_args/`),篇幅大的頁面只翻實用子集,結尾附官方連結
- 根目錄的 `quickstart.md`、`quantization-benchmark.md` 這類文件:這台機器上的實測紀錄,跟官方文件翻譯分開放
